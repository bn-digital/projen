import { Component, DependencyType, Project } from "projen"export interface I18nOptions {  readonly enabled?: boolean  readonly defaultLocale?: string  readonly locales?: string[]}export class I18n extends Component {  readonly options: I18nOptions  constructor(project: Project, options: I18nOptions = { defaultLocale: "en" }) {    super(project)    this.options = options  }  preSynthesize() {    super.preSynthesize()    this.project.deps.addDependency(`react-intl`, DependencyType.RUNTIME)  }}