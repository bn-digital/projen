# ~~ Generated by projen. To modify, edit .projenrc.ts and run "npx projen".

queue_rules:
  - name: default
    update_method: merge
    conditions:
      - -label~=(do-not-merge)
      - status-success=build
      - status-success=analyze
pull_request_rules:
  - name: Automatic merge on approval and successful build
    actions:
      delete_head_branch: { }
      queue:
        method: squash
        name: default
        commit_message_template: |-
          {{ title }} (#{{ number }})

          {{ body }}
    conditions:
      - -label~=(do-not-merge)
      - status-success=build
      - status-success=analyze
